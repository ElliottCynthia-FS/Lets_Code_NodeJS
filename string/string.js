// instead of running this:
// const uppercase = (str) => {
    // return str.toUpperCase();
// }
// Run one-line code like this:
const uppercase = (str) => str.toUpperCase();

const lowercase = (str) => str.toLowerCase();

module.exports = {
    uppercase,
    lowercase
}